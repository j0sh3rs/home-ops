machine:
  # Kernel modules loaded at boot. Kept explicitly empty so it is obvious there
  # are no extra modules to load beyond Talos defaults + system extensions.
  # Kernel command-line args are managed in talos/schematic.yaml (UKI images
  # embed cmdline; machine.install.extraKernelArgs conflicts with UKI).
  kernel:
    modules: []
