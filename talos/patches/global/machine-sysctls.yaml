machine:
  sysctls:
    # Filesystem notifications — raised for apps watching many files.
    fs.inotify.max_user_watches: "1048576"
    fs.inotify.max_user_instances: "8192"

    # Socket buffers — raised for QUIC/HTTP3 paths (Cloudflared etc).
    net.core.rmem_max: "7500000"
    net.core.wmem_max: "7500000"

    # TCP tuning — BBR + fq qdisc for LAN throughput, generous buffers.
    net.core.netdev_max_backlog: 30000
    net.core.default_qdisc: "fq"
    net.ipv4.tcp_rmem: 4096 87380 33554432
    net.ipv4.tcp_wmem: 4096 65536 33554432
    net.ipv4.tcp_fastopen: "3"
    net.ipv4.tcp_mtu_probing: "1"
    net.ipv4.tcp_tw_reuse: 1
    net.ipv4.tcp_window_scaling: 1
    net.ipv4.tcp_congestion_control: bbr

    # ARP table thresholds — cluster has many pods; defaults thrash gc.
    net.ipv4.neigh.default.gc_thresh1: "4096"
    net.ipv4.neigh.default.gc_thresh2: "8192"
    net.ipv4.neigh.default.gc_thresh3: "16384"

    # Tetragon needs readable kptr for CAP_SYSLOG attach.
    kernel.kptr_restrict: "1"
