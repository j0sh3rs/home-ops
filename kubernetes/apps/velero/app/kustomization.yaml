---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  - ./secret.sops.yaml
  # - ./helmrelease.yaml
  # Temporarily commented out until Velero CRDs are installed
  # - ./schedule.yaml
