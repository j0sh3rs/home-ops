---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./agentconfig-opencode.yaml
  - ./workspace-home-ops.yaml
  - ./secret.sops.yaml
  # task-test.yaml intentionally excluded — apply manually for testing only
