---
# AgentConfig: reusable instructions + MCP servers for OpenCode Tasks.
# agentType/model/env/resources go on the Task CR, not here.
# This CR is referenced by Task.spec.agentConfigRef.
apiVersion: kelos.dev/v1alpha2
kind: AgentConfig
metadata:
  name: opencode-local
  namespace: kelos-system
spec:
  # Custom instructions written to ~/.config/opencode/AGENTS.md at pod start.
  agentsMD: |
    # Home-ops Kelos Agent

    You are an AI coding assistant running inside the
    home-ops Kubernetes cluster.
    You have access to the local git repository mounted at /workspace/repo.

    ## Guidelines
    - Prefer minimal, targeted changes. Do not refactor beyond task scope.
    - Always run `kustomize build` to validate before considering a change done.
    - Use `task sops:verify` before any commit touching *.sops.yaml files.
    - Never modify git user config or remote URLs.

  # MCP servers available to all Tasks using this AgentConfig.
  mcpServers:
    - name: github
      type: http
      url: "http://mcpjungle.ai.svc.cluster.local:8080/github"
    - name: context7
      type: http
      url: "http://mcpjungle.ai.svc.cluster.local:8080/context7"
