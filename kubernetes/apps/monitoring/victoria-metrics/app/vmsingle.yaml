---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/operator.victoriametrics.com/vmsingle_v1beta1.json
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMSingle
metadata:
  name: vmsingle
spec:
  retentionPeriod: "30d"
  replicaCount: 1
  extraArgs:
    promscrape.maxScrapeSize: "67108864"
    # Dedup samples at scrape-level during parallel-run so both Prom remote_write
    # and vmagent-direct scrape don't double-count.
    dedup.minScrapeInterval: "1ms"
    # Query-path tuning. Default maxConcurrentRequests=16 + maxQueueDuration=10s
    # was 429-flooding clients (HA REST sensors, Grafana) whose queries queued
    # past 10s. Node is RAM-bound, so concurrency is raised only modestly;
    # the bigger lever is a longer queue window so requests wait instead of
    # getting rejected.
    search.maxConcurrentRequests: "24"
    search.maxQueueDuration: "30s"
  storage:
    accessModes: [ReadWriteOnce]
    storageClassName: openebs-hostpath
    resources:
      requests:
        storage: 100Gi
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      memory: 4Gi
  # Pin to bee-jms-03 (64GB node) — VMsingle perf is RAM-bound for indexes.
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: node.kubernetes.io/memory-tier
                operator: In
                values: [high]
  serviceScrapeSpec:
    endpoints:
      - port: http
        interval: 30s
