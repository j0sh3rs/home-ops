---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: drm-exporter
spec:
  chartRef:
    kind: OCIRepository
    name: drm-exporter
  interval: 1h
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    # All 4 nodes carry an AMD GPU
    # (feature.node.kubernetes.io/pci-0300_1002.present), unlike
    # amd-device-metrics-exporter's gpuagent which only enumerates the
    # discrete Navi 48 card on bigboi-jms-01 — the 3 Renoir/Cezanne APU
    # nodes (bee-jms-*) were previously unmonitored. drm-exporter reads
    # sysfs/DRM directly, so it covers APUs too; each pod exits cleanly
    # on a GPU-less node.
    nodeSelector:
      kubernetes.io/arch: amd64
      feature.node.kubernetes.io/pci-0300_1002.present: "true"
    hostAccess:
      # AMD-only cluster: no MSR/devCpu path needed (Intel-only), sys
      # is required.
      devDri: /dev/dri
      sys: /sys
    # AMD + sysfs stats need neither PERFMON nor SYS_RAWIO (those are
    # Intel-only paths for i915/xe perf-PMU utilization and MSR package
    # power/temp).
    securityContext:
      capabilities:
        drop:
          - ALL
        add: []
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 65534
      runAsGroup: 65534
      seccompProfile:
        type: RuntimeDefault
    resources:
      requests:
        cpu: 10m
        memory: 32Mi
      limits:
        memory: 128Mi
    service:
      type: ClusterIP
    # vmagent picks this up via its `selectAllByDefault: true` setting
    # (see kubernetes/apps/monitoring/victoria-metrics/app/vmagent.yaml).
    monitoring:
      serviceMonitor:
        enabled: true
        interval: 30s
      dashboards:
        enabled: true
        grafanaOperator:
          enabled: true
          folder: monitoring
          matchLabels:
            grafana.internal/instance: grafana
