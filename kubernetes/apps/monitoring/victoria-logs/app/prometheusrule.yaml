# Alert: VictoriaLogsNoRecentLogs
#  alerts:
#  - alert: VictoriaLogsNoRecentLogs
#    expr: sum(rate(victoria_logs_last_received[5m])) == 0
#    for: 5m
#    labels:
#      severity: warning
#    annotations:
#      summary: "No recent logs from Victoria"
#      description: "Victoria is not receiving logs in the last 5 minutes.
# Check if the application is up and running."