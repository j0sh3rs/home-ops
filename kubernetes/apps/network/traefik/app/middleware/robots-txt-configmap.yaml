---
apiVersion: v1
kind: ConfigMap
metadata:
  name: botwrangler-robots-template
  namespace: network
data:
  robots.txt: |
    {{ range $agent := .UserAgentList }}
    User-agent: {{ $agent }}
    {{- end }}
    Disallow: /
