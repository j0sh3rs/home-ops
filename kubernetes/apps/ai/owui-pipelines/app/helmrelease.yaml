---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app owui-pipelines
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    global:
      annotations:
        reloader.stakater.com/auto: "true"
    controllers:
      owui-pipelines:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/open-webui/pipelines
              # renovate: datasource=github-releases depName=open-webui/open-webui
              tag: main
              pullPolicy: Always
            env:
              PIPELINES_DIR: /pipelines
            envFrom:
              - secretRef:
                  name: owui-pipelines-secrets
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                cpu: "500m"
                memory: 512Mi
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
    service:
      app:
        controller: *app
        ports:
          http:
            port: &port 9099
    persistence:
      pipelines:
        type: configMap
        name: owui-pipelines-scripts
        globalMounts:
          - path: /pipelines
