---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./secret.sops.yaml
  # runner/job-template.yaml is NOT wired here — applied per-task
  # (manually for P1 probe, by n8n at P4). This kustomization only
  # materializes the secret so it exists when a Job runs.
