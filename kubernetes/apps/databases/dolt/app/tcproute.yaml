---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/tcproute_v1alpha2.json
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: dolt
  namespace: databases
  annotations:
    external-dns.alpha.kubernetes.io/hostname: dolt.68cc.io
    external-dns.alpha.kubernetes.io/target: 192.168.35.15
spec:
  parentRefs:
    - name: traefik-external-gateway
      namespace: network
      sectionName: mysql
  rules:
    - backendRefs:
        - name: dolt
          port: 3306
